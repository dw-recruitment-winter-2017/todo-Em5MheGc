(ns clojure_todos.views.tasks
  (:require [clojure_todos.views.layout :as layout]
            [ring.util.anti-forgery :as anti-forgery]))

(defn task-form [])

(defn display-tasks [tasks])

(defn index [tasks])
